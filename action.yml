name: "cangulo.nuke.releasecreator"
description: "Download cangulo.nuke.releasecreator to $GITHUB_WORKSPACE path"
inputs:
  version:
    description: "Version as 'x.y.z' or 'latest'"
    required: true
    default: "latest"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.version }}
